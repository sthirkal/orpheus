# orpheus
cute baby pink custom text editor using the terminal


install using the following commands
```
# cd into orpheus/
chmod +x install.sh
./install.sh

source ~/.bashrc  

# use the command "orpheus" to open a new file, shortcuts listed at the bottom of the editor
orpheus
orpheus myfile.md
```
